pipeline{
    agent any
    environment {
        NAME_APP = "webgoat"
        PORT_APP = "3333"
    }
    
    stages {
        stage('Build da imagem'){
            steps {
                git url: 'https://github.com/WebGoat/WebGoat/tree/develop/docker'
                script {
                    docker.build "$NAME_APP:latest"
                }
            }
        }
    }    
  
        stage('Executando Container'){
            steps {
                sh '''
                    if [ ! "$(curl -s localhost:$PORT_APP &> /dev/null)" ]; then
                    docker run -d --name $NAME_APP -p $PORT_APP:3333 -p 8880:8880 -p 9090:9090 -e TZ=America/Sao_Paulo $NAME_APP:latest
                    fi
                '''
            } 
        }
        
    post {
        success {
            echo "Pipeline executado com sucesso!!"
        }
        failure {
            echo "Pipeline falhou!"
        }
    }

}
